import { card } from "./card";
import { column } from "./column";
export interface board {
  name: string;
  ownerId: string;
  id: string;
  // columns
  //cards
  // other data we might need later
}
